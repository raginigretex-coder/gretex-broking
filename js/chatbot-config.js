/**
 * Gretex Chatbot - Gemini API Configuration
 * Add your Google Gemini API key below.
 * Get one free at: https://aistudio.google.com/apikey
 *
 * IMPORTANT: If this site is public, your API key will be visible in the browser.
 * For production, use a backend proxy to hide the key. For testing/demo, this is fine.
 */

// AI chatbot (Gemini) disabled - uncomment and add key to enable
// window.GRETEX_CHATBOT_CONFIG = {
//   geminiApiKey: '*****'
// };

window.GRETEX_CHATBOT_CONFIG = {
  geminiApiKey: ''
};
